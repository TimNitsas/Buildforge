<UserControl
    x:Class="Buildforge.App.Control.BuildforgeProgressBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Buildforge.App.Control"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="20"
    d:DesignWidth="200"
    mc:Ignorable="d">
    <Grid>
        <ProgressBar
            d:Value="33"
            BorderThickness="0"
            Foreground="{StaticResource Green}"
            Value="{Binding ProgressValue, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            d:Text="Progress Text"
            FontSize="10"
            Text="{Binding ProgressText, RelativeSource={RelativeSource AncestorType=UserControl}}" />
    </Grid>
</UserControl>

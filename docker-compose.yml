services:
  postgres:
    image: postgres:18.3
    restart: always
    environment:
      POSTGRES_PASSWORD: default_password
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
